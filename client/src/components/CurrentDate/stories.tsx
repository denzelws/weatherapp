import { Story, Meta } from '@storybook/react'
import CurrentDate from '.'

export default {
  title: 'CurrentDate',
  component: CurrentDate
} as Meta

export const Default: Story = () => <CurrentDate />
