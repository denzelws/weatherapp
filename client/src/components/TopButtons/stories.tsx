import { Story, Meta } from '@storybook/react'
import TopButtons from '.'

export default {
  title: 'TopButtons',
  component: TopButtons
} as Meta

export const Default: Story = () => <TopButtons />
